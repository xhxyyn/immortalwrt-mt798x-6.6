--- a/iwinfo.c
+++ b/iwinfo.c
@@ -498,6 +498,7 @@ rpc_iwinfo_add_rateinfo(struct
 		blobmsg_add_u32(&buf, "mcs", r->mcs);
 		blobmsg_add_u32(&buf, "nss", r->nss);
 		blobmsg_add_u32(&buf, "eht_gi", r->eht_gi);
+		blobmsg_add_u32(&buf, "eht_dcm", r->eht_dcm);
 	}
 }
 
